import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  footerContainer: {
    marginTop: 32,
  },
  footerText: {
    color: '#FDFCFE',
    fontSize: 14,
    textAlign: 'center',
  },
})
